<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Record Rule: All Orders limited -->
        <record id="rule_sale_order_all_orders_limited" model="ir.rule">
            <field name="name">All Contracts limited</field>
            <field name="model_id" ref="model_sale_quotations"/>

            <!-- Dominio "siempre true" -->
            <field name="domain_force">[(1,'=',1)]</field>

            <!-- Permisos (como en tu captura) -->
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_create" eval="1"/>
            <field name="perm_unlink" eval="0"/>
        </record>
    </data>
</odoo>